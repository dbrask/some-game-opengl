# Notice name prefix of this variable, set by CMake according
# to value given with "project()" in the root CMakeLists.txt.
include_directories(${SomeGame_SOURCE_DIR}/inc)
link_directories(${SomeGame_SOURCE_DIR}/inc)

